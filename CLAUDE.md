# CLAUDE.md

See **[AGENTS.md](AGENTS.md)** — one file, so the two cannot drift apart.

It points at the design in `docs/PLAN.md`, lists the decisions that are settled
(fixed 24h window, no caching, no NSFW classification, hotlinked art, the model
writes the whole document), and holds the conventions this project inherits from
its sibling relay.
